#!/bin/bash

tmux -CC attach || tmux -CC

# non-iTerm?
#if [ "$TERM" != "screen" ]
#then
#    if type tmux >/dev/null 2>&1
#    then
#        tmux att || tmux \
#            new -s Main \; \
#            neww -n ruby "pry"
#    fi
#fi
